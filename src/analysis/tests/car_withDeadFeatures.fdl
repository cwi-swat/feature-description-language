diagram car

features
  Car: all(body, Engine, Transmission, HorsePower, pulls?)
  Engine: more-of(combustion, electric)
  Transmission: one-of(manual, automatic)
  HorsePower: one-of(lowPower, mediumPower, highPower) 
  
constraints
  pulls requires highPower
  // by always including pulls we make the features low- and mediumPower dead
  include pulls